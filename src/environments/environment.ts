// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebase: {
    projectId: 'brief-ionic-app',
    appId: '1:665631817432:web:bcc77a01f1cabe10b4a1ad',
    storageBucket: 'brief-ionic-app.appspot.com',
    locationId: 'europe-west',
    apiKey: 'AIzaSyAGdts0Z_-NQCfa750Q6ukPsjMkCsvZaFY',
    authDomain: 'brief-ionic-app.firebaseapp.com',
    messagingSenderId: '665631817432',
    measurementId: 'G-1XL7VNPDMH',
  },
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
